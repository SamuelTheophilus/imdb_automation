These images are multiple views of the same product — front, back, sides, and close-ups.

Rules:
- Treat all images as one product. Combine information across all views.
- Return a single JSON object. Do not return an array.
- For barcode: find the barcode SYMBOL (vertical black bars) and read the numeric digits printed below it. Return digits only. Use "" if not legible.
- Use "" for any field not clearly visible across all images.
- Do not guess.
